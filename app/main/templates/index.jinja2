{% extends 'base.jinja2' %}

{% block title %}{{ _('Home') }} - {{ super() }}{% endblock %}

{% block content %}
    <div style="display: flex; flex-direction: column; align-items: center;">
        <h1>Hello PsychOlympia!</h1>
        {% if current_user.is_authenticated %}
            <p>Logged in as {{ current_user.username }}</p>
            <p>Has the following roles: {{ current_user.roles | map(attribute='name') | join(', ') }}</p>
            <form action="{{ url_for('auth.logout') }}" method="post">
                <input type="hidden" name="csrf_token" value="{{ csrf_token() }}">
                <button type="submit" style="display: flex; align-items: center; gap: 5px;">
                    <span class="material-symbols-outlined">logout</span>
                    Logout
                </button>
            </form>
        {% else %}
            <a href="{{ url_for('auth.login') }}" style="display: flex; align-items: center; gap: 5px; text-decoration: none;">
                <span class="material-symbols-outlined">login</span>
                Login
            </a>
        {% endif %}
        <a href="{{ url_for('main.team_map') }}">Teamkarte</a>
        {#<a href="{{ url_for('main.camp_map') }}">Campkarte</a>#}
    </div>
{% endblock %}
